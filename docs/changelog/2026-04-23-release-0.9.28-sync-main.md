# 2026-04-23 Synchronize release/0.9.28 into main

## Problem

Changes in `release/0.9.28` were not yet synchronized into `main`.

## Change

Synchronized `origin/release/0.9.28` into the main sync working branch via merge commit.

Included release-side updates brought by the merge:

- Added session OOM resume safeguards in `packages/runner-pi/src/session-utils.ts`
- Updated resume handling in `packages/runner-pi/src/pi-runner.ts`
- Added tests for session utilities in `packages/runner-pi/src/__tests__/session-utils.test.ts`
- Added changelog entry `docs/changelog/2026-04-22-session-oom-fix.md`
- Reordered imports in `packages/runner-pi/src/pi-runner.ts` to satisfy repository lint rules after merge
- Corrected `docs/changelog/2026-04-22-session-oom-fix.md` to match implementation (`extractLastCompactionSummary` reads the last 1MB)
- Removed outdated reference to `packages/benchmark-shared/tsconfig.json` from `docs/changelog/2026-04-22-session-oom-fix.md` after preserving main's benchmark package removals

## Conflict Resolution

Resolved modify/delete conflicts by preserving `main`'s removal of legacy benchmark package files:

- `packages/benchmark-sandagent/src/run-benchmark-smoking.ts`
- `packages/benchmark-shared/src/datasets/smoking.ts`
- `packages/benchmark-shared/tsconfig.json`

This keeps the current benchmark structure in `main` intact while applying the release fixes.
